# ── LiveKit Agents Framework ──
livekit-agents>=1.0
livekit-plugins-openai>=1.0
livekit-plugins-cartesia>=1.0
livekit-plugins-gladia>=1.0
livekit-plugins-silero>=1.0
livekit-plugins-turn-detector>=1.0   # End-of-turn detection (reduces latency)

# ── HTTP & API ──
httpx>=0.27
python-dotenv>=1.0
fastapi>=0.111
uvicorn>=0.30

# ── Data & Config ──
pydantic>=2.7
pydantic-settings>=2.3
numpy>=1.26

# ── LiveKit API (Server-side tokens) ──
livekit-api>=0.1.0

# ── OpenAI SDK (for Embeddings + Post-call Summaries) ──
openai>=1.30
